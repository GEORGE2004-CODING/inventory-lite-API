spring:
  application:
    name: API-inventory

  datasource:
    url: jdbc:mysql://localhost:3306/inventory_database
    username: root
    password: manuelin2004
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
